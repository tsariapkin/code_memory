---
name: index-project
description: Index all Python files in the project to build the symbol database
disable-model-invocation: true
---

Call the `index_project` MCP tool now to parse all Python files in the current project. This builds the symbol index (functions, classes, methods, imports) and dependency graph.

After indexing completes, report a summary of what was indexed.
